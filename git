//git learner 
to configure the username and email
command (git config --global  user.name "" user.email "";
next to connet the git repostory in the system git 
git is verion control and its is a backbone of the all applicaton and software .the main role of git is to control the version
//
